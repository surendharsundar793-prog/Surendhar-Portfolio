apiVersion: v1
kind: Service
metadata:
  name: nodeport-svc
spec:
  selector:
    app: demo
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 155
      targetPort: 80
      # nodePort: 30005